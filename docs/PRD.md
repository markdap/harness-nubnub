# PRD: {프로젝트명}

<!--
이 파일은 새 프로젝트 시작 시 AI와 대화하며 채우는 템플릿이다.
/harness-write가 그릴 결과를 자동 분배해 채운다.
matt-pocock /to-prd의 좋은 부분(Out of Scope / 구현 결정 / 테스트 접근)을
흡수하되 한국어 lean 형식 유지.

채우는 순서: PRD → ARCHITECTURE → ADR → CLAUDE.md (이 순서가 중요. 뒤에 오는 파일은 앞에서 파생됨).
execute.py가 자동으로 읽어 각 step 프롬프트에 맥락으로 주입한다.

후속 호출 시 (이미 채워져 있을 때):
- 맨 위에 새 ## v{N+1} 섹션 추가
- 옛 섹션은 *superseded* 태그로 보존
- 새 섹션 안에 "Affects" 영향 분석 메타 명시 강제 (ARCH/ADR/CLAUDE 변동 여부)
-->

## 목표 (Problem + Solution)
<!-- 한두 줄. 문제와 그 해결을 사용자 관점에서 묶어 서술.
matt-pocock Problem Statement + Solution 결합형.
예: "출근 의무가 강한 직장인이 알람을 끄고 다시 잠드는 문제를, 가속도+사진 검증으로 진짜 일어나야 알람이 꺼지는 방식으로 해결한다." -->
{이 프로젝트가 해결하려는 문제와 해결 방식을 한두 줄로}

## 사용자
<!-- 누가 이 제품을 쓰는지. 페르소나.
예: "20-40대 직장인. 출근 시간 엄격. 자기 의지로 못 일어나는 자각 있음." -->
{누가 이 제품을 쓰는지}

## 핵심 기능
<!-- MVP에서 반드시 있어야 할 기능. 3개 이하가 이상적. -->
1. {기능 1}
2. {기능 2}
3. {기능 3}

## Out of Scope (MVP 제외 사항)
<!--
중요: "안 만들 것"을 명시하지 않으면 AI가 "이 기능도 추가할까요?" 한다.
matt-pocock /to-prd 정통 흡수.
비어있어도 빈 칸 X — "현재 명시된 제외 사항 없음"이라고 적음.
-->
- {안 만들 것 1 (예: 팀 협업 기능 — v2에서)}
- {안 만들 것 2 (예: 모바일 앱 — 웹으로만)}

## 디자인 방향
<!-- harness 기존. UI_GUIDE.md 있으면 여기서 참조. -->
- {디자인 방향 (예: 다크모드 고정, 미니멀, 한국어 우선)}
- {색상 (예: 무채색 + 포인트 1가지)}

## (선택) 구현 결정
<!--
matt-pocock /to-prd Implementation Decisions에서 흡수.
그릴에서 모듈/인터페이스/스키마가 결정되면 여기 적음.
없으면 이 섹션 자체 생략 가능.
파일 경로나 코드 스니펫 X — 빨리 stale됨.
예외: prototype에서 나온 결정-rich한 snippet (state machine, schema, type shape) 인라인 가능.
-->
- {결정 1: 어떤 모듈 / 어떤 인터페이스로 갈지}

## (선택) 테스트 접근
<!--
matt-pocock /to-prd Testing Decisions에서 흡수.
어떤 모듈이 테스트 가치 있는지, 비슷한 prior art는 어디 있는지.
CLAUDE.md의 CRITICAL: TDD 룰과 보완.
-->
- {테스트 가치가 큰 영역}
- {prior art (있으면)}
